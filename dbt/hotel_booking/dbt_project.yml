name: hotel_booking
version: "1.0.0"
config-version: 2

profile: hotel_booking

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hotel_booking:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: view
    materialized_views:
      +materialized: starrocks_materialized_view
